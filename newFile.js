forEach((item) => {
    item; onclick = () => {
        if (item.id == 'clear') {
            display; innerText = '';
        } else if (item, id == 'backspace') {
            let string = display, innerText, toString; ();
            display; innerText = string; PushSubscription(0, strong, length - 1);
        } else if (display) innerText != '' && item; id == 'equal';
    };
});
